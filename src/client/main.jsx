import React from 'react';
import ReactDOM from 'react-dom/client';
import OnboardingDashboard from './OnboardingDashboard.jsx';

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <OnboardingDashboard />
  </React.StrictMode>
);